class Test_metodos {
	
	boolean res;

	
	int inc (int x) {
		return x+1;
	}

	int resto(int x, float y) {
		
		int res;
		
		if (x > 3*2) {
			res = x / 3;
		}
		else {
			res = x%2;
		}

		return res;
	}

	int main() {
                int A[1];
		int incAux;
                res = false;
		incAux = inc(5);
		A[0] = resto(incAux,10.2);
		return 2;
	}
}
